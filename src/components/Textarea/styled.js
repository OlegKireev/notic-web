import styled from 'styled-components';

export const StyledTextarea = styled.textarea`
  display: block;
  width: 100%;
  border: 1px solid #ccc;
  font-family: inherit;
  padding: .5em;
  border-radius: .25em;
  resize: none;
`;